备注：
    每个用户在登记的时候产生一个用户信息 将该用户信息存在数据库中
     姓名 性别 身份证（16位）手机号 是否有车（1，0） 门牌号
     门牌号 - 车牌号 车位

bean - mapper - xml
一个bean实例 对应一个数据库表 ，对应一个 mapper 接口，对应一个xml文件
数据库表：
    redis数据库：
        0号数据库存放用户信息
        1号数据库存放管理员信息（有一个超级管理员）

    mysql数据库：
        管理员表：
          账户、密码、姓名、性别、身份证号、手机号、创建时间、修改时间
        住户表：
          姓名、性别、身份证号、手机号、是否有车（1，0）、门牌号
        车位表：
          门牌号 车牌号 车位
        维修师傅表：
          账户、密码、姓名、性别、身份证号、手机号、技能
        留言表：
          姓名、留言时间、留言内容
        报修表：
          姓名、门牌号、报修内容、是否接单、接单人、预约维修时间
